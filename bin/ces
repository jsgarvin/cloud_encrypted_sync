#!/usr/bin/env ruby
require 'optparse'
options = {}
option_parser = OptionParser.new do |opts|
  opts.on('-n NAME','--name') do |name|
    options[:name] = name
  end
end
option_parser.parse!
puts "Hello, #{options[:name] || 'World'}!"